# Projects

Your default home for **code and git repositories**. Clone or create projects
here; this is the directory VS Code and the terminal open into by default.

```
cd ~/Documents/Projects
git clone <repo>
code .
```
