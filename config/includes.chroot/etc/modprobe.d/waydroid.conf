# No BINDERFS on this kernel, so binder_linux must create the legacy device
# nodes itself. Without these params there is no /dev/binder* and Waydroid
# (waydroid init / waydroid-container) fails to start.
options binder_linux devices=binder,hwbinder,vndbinder
