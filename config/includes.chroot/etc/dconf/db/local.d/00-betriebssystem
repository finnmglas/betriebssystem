# System-wide GNOME defaults for BETRIEBSSYSTEM.
# Applied by the 0300-dconf-update chroot hook (`dconf update`).

[org/gnome/desktop/background]
picture-uri='file:///usr/share/backgrounds/betriebssystem/wallpaper.png'
picture-uri-dark='file:///usr/share/backgrounds/betriebssystem/wallpaper.png'
picture-options='zoom'
primary-color='#000000'
secondary-color='#000000'

[org/gnome/desktop/screensaver]
picture-uri='file:///usr/share/backgrounds/betriebssystem/wallpaper.png'
picture-options='zoom'
primary-color='#000000'

[org/gnome/desktop/interface]
color-scheme='prefer-dark'
gtk-theme='Adwaita-dark'
enable-animations=true
enable-hot-corners=true
clock-show-weekday=true
show-battery-percentage=true

[org/gnome/mutter]
edge-tiling=true
dynamic-workspaces=true

[org/gnome/desktop/input-sources]
# German keyboard layout; system language stays English (en_US.UTF-8).
sources=[('xkb', 'de')]
xkb-options=['terminate:ctrl_alt_bksp']

[org/gnome/nautilus/preferences]
default-folder-viewer='list-view'
show-hidden-files=true
show-delete-permanently=true
show-create-link=true
# Double-click to open everything (folders + files). tap-to-click is on, so a
# double-tap on the touchpad = double-click.
click-policy='double'
# Sort by modification time, oldest first.
default-sort-order='mtime'
default-sort-in-reverse-order=false
recursive-search='always'
show-image-thumbnails='always'

[org/gnome/nautilus/list-view]
use-tree-view=false
default-zoom-level='small'
default-visible-columns=['name', 'size', 'type', 'date_modified']

[org/gtk/settings/file-chooser]
show-hidden=true
sort-directories-first=true

[org/gtk/gtk4/settings/file-chooser]
show-hidden=true
sort-directories-first=true

[org/gnome/desktop/peripherals/touchpad]
tap-to-click=true
natural-scroll=true

[org/gnome/login-screen]
logo='/usr/share/pixmaps/betriebssystem-logo.png'
disable-user-list=false

[org/gnome/shell]
# Dash favorites, in order: Firefox, Files, Logseq, VS Code, Android Studio,
# Disks, Task Manager (System Monitor), Terminal. Logseq/Android Studio arrive
# via the first-boot Flatpak/tarball install; GNOME silently ignores a favorite
# whose .desktop is not (yet) present.
favorite-apps=['firefox-esr.desktop', 'org.gnome.Nautilus.desktop', 'com.logseq.Logseq.desktop', 'code.desktop', 'com.google.AndroidStudio.desktop', 'org.gnome.DiskUtility.desktop', 'org.gnome.SystemMonitor.desktop', 'org.gnome.Terminal.desktop']
# Enabled extensions. GNOME silently ignores any that aren't installed or aren't
# compatible with this shell version, so a missing/broken one never breaks the
# session. (Blur my Shell is packaged; the search/notification ones are fetched
# by the 0230-gnome-extensions hook for shell 48 -- skipped silently if absent.)
enabled-extensions=['tiling-assistant@leleat-on-github', 'blur-my-shell@aunetx', 'dash-to-dock@micxgx.gmail.com', 'windowIsReady_Remover@nunofarruca@gmail.com', 'folder-search-provider@sitnik.ru', 'BrowserSearchProvider@mepowerleo10.github.io']
disable-extension-version-validation=true

[org/gnome/shell/extensions/dash-to-dock]
# Bottom dock visible on an empty desktop, auto-hiding only when a window would
# overlap it ("show the dock when no apps are active").
dock-position='BOTTOM'
dock-fixed=false
autohide=true
intellihide=true
intellihide-mode='ALL_WINDOWS'
extend-height=false
show-apps-at-top=true
click-action='minimize-or-overview'
running-indicator-style='DOTS'
